name: Lacework Image Scan from Dockerfile

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  scan:
    runs-on: ubuntu-latest
    env:
      LW_ACCOUNT_NAME: ${{ secrets.LW_ACCOUNT_NAME }}
      LW_ACCESS_TOKEN: ${{ secrets.LW_ACCESS_TOKEN }}

    steps:
      - name: Checkout source
        uses: actions/checkout@v3

      - name: Build Docker image locally
        run: |
          docker build -t myapp:latest .

      - name: Install Lacework Inline Scanner
        run: |
          curl -L https://github.com/lacework/lacework-vulnerability-scanner/releases/latest/download/lw-scanner-linux-amd64 -o lw-scanner
          chmod +x lw-scanner
          sudo mv lw-scanner /usr/local/bin/lw-scanner

      - name: Scan local image for vulnerabilities
        run: |
          lw-scanner image evaluate myapp:latest --upload
